all:
  hosts:
    yt-prod-server:
      ansible_host: i-08b1ce03a3680e7bf
      ansible_user: ec2-user
      ansible_ssh_private_key_file: /home/ahmed/.ssh/Keypairforytthumbnail.pem
      ansible_ssh_common_args: >-
        -o StrictHostKeyChecking=no
        -o UserKnownHostsFile=/dev/null
        -o ProxyCommand="AWS_SHARED_CREDENTIALS_FILE=/mnt/c/Users/Admin/.aws/credentials /usr/local/bin/aws ssm start-session --target %h --document-name AWS-StartSSMSession --parameters portNumber=%p --region us-east-1"
