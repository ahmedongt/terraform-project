plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: [running]
hostnames:
  - ip-address
compose:
  ansible_user: ubuntu
  ansible_ssh_private_key_file: ~/.ssh/Keypairforytthumbnail.pem
