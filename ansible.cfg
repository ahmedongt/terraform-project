[defaults]
gathering = smart
host_key_checking = False

[ssh_connection]
# CRITICAL SPEED BOOST: Streams tasks directly into memory over your SSM proxy 
# instead of performing 15 separate slow file uploads.
pipelining = True
ssh_args = -o ControlMaster=auto -o ControlPersist=120s -o StrictHostKeyChecking=no
# FIX: Directs execution behaviors to transfer cleanly if an explicit tty channel isn't established
scp_if_ssh = True